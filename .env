RTSP_URL=

# MODEL_PATH — two options:
#   Option A (PyTorch, default): works on any GPU, no export step needed
MODEL_PATH=best.pt
#   Option B (TensorRT, faster): export first (see README or API docs), then switch to:
# MODEL_PATH=best.engine

ROI_POSITION=0.7
CONFIDENCE=0.25
NMS_IOU=0.45
IMGSZ=640
MAX_DISTANCE=50
MAX_DISAPPEARED=15
